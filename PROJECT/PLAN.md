* Что улучшить перед продом:
* *Безопасность: закрыть все админ/сервисные API авторизацией, маскировать ключи в логах, валидация входов.*
* **Надёжность: таймауты/ретраи для OpenAI и Qdrant, централизованная обработка ошибок, health-check /api/health.**
* Хранилище: если ожидается параллельная загрузка/рост — рассмотреть миграцию с SQLite на Postgres; бэкапы БД и uploads/.
* Индексация: защита от повторного запуска синка (идемпотентность), блокировки на файл, ограничение параллелизма (пока синхронно — ок).
* Наблюдаемость: структурные логи (request-id), уровень логов по env, сбор метрик (время эмбеддинга/поиска).
* UX/операторские: кнопка переиндексации одного документа, просмотр последних ошибок синка.
* Перфоманс: настройки embedding_batch_size/rate-limit объединить с глобальным RPM, лимиты на размер запроса в /api/ask*, нарезка слишком длинных запросов.
* Векторная БД: в stats уже показываем named vectors; добавить проверку совпадения размеров модели/коллекции при старте.
* Файлы: белый список MIME/расширений уже есть; добавить антивирус (по желанию) и удаление временных.
