{
  "AI_Model_and_Response_Generation": {
    "title": "🧠 AI Модель и генерация ответов",
    "settings": {
      "model": {
        "description": "OpenAI модель, используемая для генерации (например, gpt-4o)",
        "default": "gpt-4o"
      },
      "temperature": {
        "description": "Контролирует степень креативности ответов: от 0 (точно) до 1 (разнообразно)",
        "default": 0.4
      },
      "max_tokens": {
        "description": "Максимальное количество токенов в ответе от модели",
        "default": 4000
      },
      "system_prompt": {
        "description": "Общее системное указание для модели (тональность, стиль)",
        "default": "Ты — духовный и мудрый ассистент, работающий на основе Учения Вознесённых Владык. Это Учение является драгоценным духовным источником, требующим бережного отношения. В своих ответах ты стремишься как можно чаще цитировать первоисточник дословно, сохраняя стиль и суть передаваемого послания. Избегай пересказов и интерпретаций, если можно привести прямую цитату. Все выводы, пояснения и рекомендации ты делаешь исключительно на основе материалов Учения, строго следуя их содержанию. Не добавляй ничего от себя."
      },
      "spiritual_prompt": {
        "description": "Дополнительная инструкция для духовной направленности",
        "default": true
      },
      "use_mock_mode": {
        "description": "Использовать режим заглушки вместо реального запроса к OpenAI",
        "default": false
      }
    }
  },
  "RAG_Embedding_and_Chunking": {
    "title": "📖 RAG: Подготовка и индексация",
    "settings": {
      "chunk_size": {
        "description": "Размер одного блока текста (в токенах), используемого для эмбеддинга",
        "default": 1000
      },
      "chunk_overlap": {
        "description": "Перекрытие между блоками текста, чтобы сохранить связность",
        "default": 200
      },
      "embedding_model": {
        "description": "Модель эмбеддинга, используемая для преобразования текста в векторы",
        "default": "text-embedding-ada-002"
      },
      "embedding_batch_size": {
        "description": "Количество чанков, обрабатываемых за один проход при эмбеддинге",
        "default": 10
      },
      "max_chunks_per_file": {
        "description": "Максимальное количество чанков, извлекаемых из одного файла",
        "default": 50
      },
      "preserve_text_structure": {
        "description": "Сохранять структуру текста при разбиении на чанки",
        "default": true
      }
    }
  },
  "Retrieval_Settings": {
    "title": "🔍 Поиск и подбор контекста",
    "settings": {
      "retrieval_k": {
        "description": "Сколько наиболее релевантных фрагментов извлекать из базы (K)",
        "default": 8
      },
      "score_threshold": {
        "description": "Минимальный порог схожести для включения фрагмента в ответ",
        "default": 0.3
      },
      "enable_re_ranking": {
        "description": "Включить повторную сортировку результатов на основе модели",
        "default": true
      },
      "context_max_length": {
        "description": "Максимальный объём текста, передаваемый в модель из найденного контекста",
        "default": 8000
      }
    }
  },
  "System_and_Limits": {
    "title": "⚙️ Система и лимиты",
    "settings": {
      "requests_per_minute": {
        "description": "Максимальное количество запросов к системе в минуту",
        "default": 60
      },
      "max_file_size_mb": {
        "description": "Максимально допустимый размер загружаемого файла в мегабайтах",
        "default": 5000
      }
    }
  }
}
