# Миграция к новым дефолтным значениям - ЗАВЕРШЕНА

## 📋 Обзор

Все настройки системы были обновлены согласно файлу `rag_settings_structure_with_defaults.json`.

## ✅ Результат миграции

### 🧠 AI Модель и генерация ответов (6 параметров)
- `openai_chat_model`: **gpt-4o** (было: gpt-4o)
- `temperature`: **0.4** (было: 0.4)  
- `max_tokens`: **4000** (было: 4000)
- `system_prompt`: **"Ты — мудрый и сочувствующий духовный ассистент..."**
- `spiritual_prompt_enabled`: **""** (пустая строка)
- `use_mock`: **false** (было: false)

### 📖 RAG: Подготовка и индексация (6 параметров)
- `chunk_size`: **1000** (было: 1000)
- `chunk_overlap`: **200** (было: 200)
- `embedding_model`: **text-embedding-ada-002** (было: text-embedding-3-small)
- `embedding_batch_size`: **10** (новый параметр)
- `max_chunks_per_file`: **50** (было: 100)
- `preserve_text_structure`: **true** (новый параметр)

### 🔍 Поиск и подбор контекста (4 параметра)
- `retrieval_k`: **8** (было: 8)
- `score_threshold`: **0.3** (было: 0.3)
- `rerank_enabled`: **true** (было: true)  
- `context_max_length`: **8000** (было: 8000)

### ⚙️ Система и лимиты (2 параметра)
- `requests_per_minute`: **60** (было: 60)
- `max_file_size_mb`: **5000** (было: 50) ⚠️ **ЗНАЧИТЕЛЬНОЕ УВЕЛИЧЕНИЕ!**

## 🔄 Что было сделано

1. **Создан скрипт миграции** `migrate-to-new-defaults.ts`
2. **Обновлены все значения** согласно JSON файлу
3. **Обновлены дефолтные значения** для всех параметров
4. **Добавлены записи в историю изменений** для аудита
5. **Удалены старые дублирующиеся категории**
6. **Очищена база данных** от лишних записей

## ⚠️ Важные изменения

### Значительные изменения значений:
- **`max_file_size_mb`**: 50 MB → **5000 MB (5 GB)** - увеличение лимита размера файлов в 100 раз!
- **`max_chunks_per_file`**: 100 → **50** - уменьшение лимита чанков на файл
- **`embedding_model`**: text-embedding-3-small → **text-embedding-ada-002** - изменение модели эмбеддинга

### Новые параметры:
- **`embedding_batch_size`**: 10 - размер пакета для обработки эмбеддингов
- **`preserve_text_structure`**: true - сохранение структуры текста при чанкинге

## 🎯 Финальная структура

**Всего параметров**: 18  
**Категорий**: 4  
**Статус**: ✅ Все значения соответствуют JSON спецификации

## 📊 Статистика миграции

- ✅ **Успешно обновлено**: 18 параметров
- ❌ **Ошибок**: 0  
- 🗑️ **Удалено старых категорий**: 5 (ai, content, search, security, system)
- 📝 **Добавлено записей в историю**: 18

## 🔄 Следующие шаги

1. **Перезапустить сервер** для применения новых настроек
2. **Проверить работу RAG системы** с новыми параметрами
3. **Протестировать загрузку файлов** с новым лимитом 5GB
4. **Проверить работу эмбеддинга** с моделью text-embedding-ada-002

---
*Миграция выполнена: $(date)*
