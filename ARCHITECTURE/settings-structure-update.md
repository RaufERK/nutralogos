# Обновление структуры настроек RAG системы

## 📋 Обзор изменений

Система настроек была реорганизована согласно новой логической структуре из `rag_settings_structure.json`.

## 🔄 Новая структура категорий

### 1. 🧠 AI Модель и генерация ответов
**Категория:** `AI_Model_and_Response_Generation`

- `openai_chat_model` - OpenAI модель для генерации
- `temperature` - Степень креативности ответов (0-1)
- `max_tokens` - Максимальная длина ответа
- `system_prompt` - Системные инструкции для AI
- `spiritual_prompt_enabled` - Духовная направленность
- `use_mock` - Режим заглушки для тестирования

### 2. 📖 RAG: Подготовка и индексация
**Категория:** `RAG_Embedding_and_Chunking`

- `chunk_size` - Размер блока текста для эмбеддинга
- `chunk_overlap` - Перекрытие между блоками
- `embedding_model` - Модель для создания векторов *(новый)*
- `embedding_batch_size` - Размер пакета для обработки *(новый)*
- `max_chunks_per_file` - Лимит чанков на файл *(новый)*
- `preserve_text_structure` - Сохранение структуры текста *(новый)*

### 3. 🔍 Поиск и подбор контекста
**Категория:** `Retrieval_Settings`

- `retrieval_k` - Количество извлекаемых фрагментов
- `score_threshold` - Минимальный порог схожести
- `rerank_enabled` - Повторная сортировка результатов
- `context_max_length` - Максимальный объем контекста

### 4. ⚙️ Система и лимиты
**Категория:** `System_and_Limits`

- `requests_per_minute` - Лимит запросов в минуту
- `max_file_size_mb` - Максимальный размер файла

## 🛠️ Технические изменения

### Обновленные файлы:
1. **`src/app/admin/settings/page.tsx`**
   - Обновлены функции `getCategoryIcon()`, `getCategoryTitle()`, `getCategoryBgColor()`
   - Добавлена поддержка новых категорий
   - Сохранена обратная совместимость со старыми категориями

2. **`scripts/update-categories-structure.ts`** *(новый)*
   - Скрипт для миграции данных в базе
   - Обновление категорий и описаний параметров
   - Добавление новых параметров

### База данных:
- Обновлены категории для всех существующих параметров
- Добавлены 4 новых параметра для эмбеддинга
- Обновлены описания и подсказки для всех параметров

## 🎨 UI улучшения

- **Цветовая схема:**
  - 🧠 AI: Фиолетовый (`bg-purple-900/90`)
  - 📖 RAG: Синий (`bg-blue-900/90`)
  - 🔍 Поиск: Бирюзовый (`bg-teal-800/90`)
  - ⚙️ Система: Изумрудный (`bg-emerald-800/90`)

- **Иконки:** Обновлены согласно новой логике категорий
- **Названия:** Более описательные и понятные заголовки

## 🔄 Миграция

Для применения изменений был выполнен скрипт:
```bash
npx tsx scripts/update-categories-structure.ts
```

**Результат миграции:**
- ✅ Обновлено 14 существующих параметров
- ✅ Добавлено 4 новых параметра
- ✅ Всего: 18 параметров в 4 категориях

## 🧪 Тестирование

После миграции рекомендуется:
1. Проверить отображение всех категорий в админ-панели
2. Убедиться в корректной работе сохранения настроек
3. Проверить, что RAG система использует обновленные параметры

## 📚 Обратная совместимость

Система сохраняет поддержку старых названий категорий для плавного перехода:
- `ai` → `AI_Model_and_Response_Generation`
- `search` → `Retrieval_Settings`
- `content` → разделено между AI и Retrieval
- `system` → разделено между AI и RAG
- `security` → `System_and_Limits`
